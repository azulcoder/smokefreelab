# Pinned for reproducible HF Spaces builds.
# Keep this list minimal — only what app.py actually imports at runtime.
streamlit==1.56.0
numpy==1.26.4
pandas==2.2.3
scipy==1.13.1
statsmodels==0.14.4
plotly==5.24.1
